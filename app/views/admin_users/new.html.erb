<%= link_to("<<< Back to Main Page", {:controller =>'acess', :action => 'index', :class => 'back-link'}) %>

<div class="Admin new">
	<h2>Create Admin </h2>

	<%= form_for(:admin_user, :url => { :action => 'create'}) do |f| %>

		<%= render(:partial => "admin", :locals => {:f => f}) %>
		<div class="form-buttons">
			<%= submit_tag("Create Admin")%>
		</div>
		<% end %>
	</div>
