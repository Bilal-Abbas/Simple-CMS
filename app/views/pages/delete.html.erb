<%= link_to("<<< Back to List", {:action => 'index', :subject_id => @subject.id, :class => 'back-link'}) %>

<div class="Page delete">
	<h2>Delete Page </h2>

	<%= form_for(:page, :url => {:action => 'destroy', :id => @page.id, :subject_id => @subject.id}) do |f| %>

		<p> Are you want to delete the <%=  @page.name %> Page??? </p>


		<div class="form-buttons">
			<%= submit_tag("Delete Page") %>
		</div>
		<% end %>
	</div>
